name: "v762-npm-single"
description: "OCCT v7.6.2 from npm (replicad-opencascadejs@0.20.2)"

compilation:
  optimization: "unknown"
  lto: false
  exceptions: "none"
  threading: "single-threaded"

linking:
  yaml: "unknown (npm published)"
  wasmOptLevel: "unknown"

benchmark:
  iterations: 10
  filter: []
