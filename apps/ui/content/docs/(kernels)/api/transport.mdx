---
title: Transport API
description: KernelTransport interface and createWorkerTransport for worker communication.
icon: lucide:cable
---

# Transport API

Use the transport API when configuring how the kernel client communicates with worker processes. The transport abstracts MessagePort-based RPC over the worker boundary.

## Types

<auto-type-table path='./props/transport.ts' name='KernelTransport' />

## createWorkerTransport

`createWorkerTransport(workerUrl)` creates a `KernelTransport` that spawns a Web Worker from the given URL and establishes a MessageChannel for bidirectional communication. Returns `KernelTransport & { worker: Worker }`.

## Usage

```typescript
import { createKernelClient, fromMemoryFS } from '@taucad/runtime';
import { replicad } from '@taucad/runtime/kernels';
import { esbuild } from '@taucad/runtime/bundler';
import { createWorkerTransport } from '@taucad/runtime/transport';

const transport = createWorkerTransport(new URL('@taucad/runtime/worker', import.meta.url).href);

const client = createKernelClient({
  kernels: [replicad()],
  bundlers: [esbuild()],
  fileSystem: fromMemoryFS(),
  transport,
});
```

## Related

- [Worker Model](/docs/concepts/worker-model)
