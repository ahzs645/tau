# AGENTS.md

## Commands

```bash
pnpm nx lint <project>                    # Lint (oxlint then eslint)
pnpm nx lint <project> --files=<path>     # Lint specific file(s) or glob
pnpm nx test <project> --watch=false      # Test
pnpm nx typecheck <project>              # Typecheck
pnpm nx build <project>                  # Build

pnpm infra:up / infra:down / infra:reset  # PostgreSQL + Redis (Docker)
pnpm db:generate                          # Generate Drizzle migrations
pnpm db:migrate                           # Run migrations
pnpm ci:affected                          # CI: affected tests, builds, lint, typecheck
pnpm docs:validate                        # Validate policy/research doc frontmatter
```

## Architecture

Tau is the AI-native CAD platform for the web (`tau.new`), built as an Nx monorepo with pnpm workspaces.

- **Frontend**: React Router v7, React 19, TypeScript, Tailwind CSS, Fumadocs
- **Backend**: NestJS API with Fastify, PostgreSQL (Drizzle ORM), Redis, Better Auth
- **CAD Engine**: Multi-kernel runtime (Replicad, JSCAD, Manifold, OpenSCAD, KCL)
- **AI**: LangGraph agent with tool-use (OpenAI, Anthropic, Vertex AI, Ollama, Together AI, Cerebras)

### Project Map

| Path                    | Description                                                                          |
| ----------------------- | ------------------------------------------------------------------------------------ |
| `apps/ui`               | React Router v7 web app (CAD editor, file manager, AI chat, docs)                    |
| `apps/api`              | NestJS API (auth, database, chat WebSocket, LangGraph agent)                         |
| `packages/runtime`      | Multi-kernel CAD runtime — consumed as source via package.json exports               |
| `packages/react`        | React hooks for `@taucad/runtime` (useRender, useGeometryExport)                     |
| `packages/cli`          | `@taucad/cli` — headless CAD CLI (`taucad export <file> --ext=glb`)                  |
| `packages/converter`    | CAD file conversion (STL, STEP, IGES, DXF, glTF, USDZ)                               |
| `packages/testing`      | Geometry analysis, grading, and test utilities (`analyzeGlb`, `evaluateRequirement`) |
| `packages/memory`       | Shared-memory primitives (`SharedPool`, `SharedMemoryArena`)                         |
| `packages/telemetry`    | OTEL metric definitions, ingest schemas, observability runtime middleware            |
| `packages/json-schema`  | JSON to JSON Schema inference                                                        |
| `libs/chat`             | AI chat tool schemas, message schemas, RPC definitions                               |
| `libs/types`            | Shared TypeScript types (API, project, CAD, file, graphics)                          |
| `libs/utils`            | Shared utilities (ID generation, path, file, schema, dispose)                        |
| `libs/units`            | Units of measurement and conversions                                                 |
| `apps/ui/content/docs/` | Docs site (Fumadocs): `(runtime)/` and `(editor)/` sections                          |

## Skills

Project skills in `.cursor/skills/` provide guided workflows. Read the relevant `SKILL.md` when performing these tasks:

| Skill                   | When to use                                                                                |
| ----------------------- | ------------------------------------------------------------------------------------------ |
| `create-policy`         | Writing or updating `docs/policy/*.md` documents                                           |
| `create-research`       | Writing or updating `docs/research/*.md` investigation documents                           |
| `adding-tools`          | Adding new tools to the AI chat system                                                     |
| `create-package`        | Scaffolding new `@taucad/*` packages via workspace generator                               |
| `create-vite-plugin`    | Adding a Vite plugin to `@taucad/vite`                                                     |
| `new-kernel`            | Adding a first-party CAD kernel to `@taucad/runtime`                                       |
| `package-release`       | Versioning, building, publishing `@taucad/*` packages                                      |
| `repos`                 | Investigating dependency source code; cloning, adding, or exploring repos via `repos.yaml` |
| `submit-pr`             | Submitting draft PRs to upstream dependency forks                                          |
| `pr-review-coordinator` | Fixing PR review comments from GitHub                                                      |
| `typescript-overloads`  | Resolving TS2322 overloaded function type errors                                           |
| `langgraph`             | Questions about LangGraph and agentic AI                                                   |
| `occt-wasm-build`       | Building OpenCASCADE WASM binaries                                                         |

## Conventions

- Early returns to reduce nesting
- Composition over inheritance; functional programming patterns preferred
- Const declarations over function declarations
- `cn()`/`clsx` for conditional classNames, not ternary
- Max 3 parameters per function; bundle extras into an options object
- Vitest for tests; jsdom env for UI, node env for API
- Hybrid oxlint + ESLint linting; formatting via oxfmt (`.oxfmtrc.json`), not ESLint
- PostgreSQL with Drizzle ORM; schema in `apps/api/app/database/`; auth tables via Better Auth
- Investigate dependency source via `repos/` (managed by `repos.yaml` and `pnpm repos`), not `node_modules`. Use the `repos` skill to clone, add, or explore repos.

## Learned User Preferences

- Write failing tests first (TDD), then fix to pass; preserve existing tests; make minimal, targeted changes and run typecheck before considering done; remove tests for non-shipped APIs/behaviors rather than skipping them; do not mock Radix `Collapsible`/portal-rendered components in jsdom — find and fix the real reason the test sees the wrong initial state instead of mocking around it
- State machines own lifecycle and state logic; UI clients send events only and never decide open/close; avoid ref/state for sync guards
- Follow policy docs when applicable: testing-policy, library-api-policy, xstate-policy, lint-policy, react-testing-policy, filesystem-policy, commit-policy, typescript-policy, agents-md-policy, context-engineering-policy, filesystem-context-policy, jsdoc-policy, documentation-policy
- Pin GitHub/dependency versions to exact commit hashes for reproducibility; use `pnpm patch` for dependency patches, do not manually create patch files; when behavior regressed from something that previously worked, prefer config changes over code changes — find the regression; replicad source/replicad-opencascadejs edits in `repos/replicad` are in scope (close collaboration with upstream maintainer); fork remotes track upstream's primary branch — assimp uses `master` (not main), tinyusdz uses `release` (not main); push fork commits to the matching upstream branch name; never hand-write full SHAs from short forms — always run `git rev-parse <short>` to obtain the canonical full hash before pinning in CMakeLists/submodule configs (synthesised back-halves silently produce non-existent commits that break `FetchContent_Declare` at configure time); commit messages on upstream/external repos must describe the change itself — never embed local research doc IDs (`R1`, `R5`, `R8`, etc.) since other contributors don't have access to those references
- Use `react-virtuoso` for virtualization, not `@tanstack/react-virtual`; follow patterns in `combobox-responsive.tsx`
- Never blow away the entire IndexedDB database — user work is stored there
- Prefer algorithmic, code-level solutions over bundler config or Vite plugins; middleware and transforms must be provider-agnostic (no model/provider-specific branching) — handle all providers generically; text normalization/sanitization belongs at the API layer (middleware/transforms), not in UI components; optimize for 3rd-party consumer DX; CAD export should not hardcode a single interchange format (e.g. GLB-only) or embed `@taucad/converter` as an internal runtime dependency — treat converters and third-party exporters (e.g. API-based format transformers) as injectable plugins with multiple providers allowed concurrently; agent-surface APIs (RPCs, tool inputs, system prompts) require explicit parameters with no silent `?? mainEntryFile`-style fallbacks so the LLM must commit to the file/target it intends; cross-cutting agent UI metadata (e.g. reasoning timing) namespaced under the generic `common` AI-SDK provider key — not a per-product `taucad` key — and typed end-to-end via `MyUIMessage` schemas (never `unknown` providerMetadata)
- Avoid type assertion escape hatches (`as never`, `as unknown as`, unnecessary `as const` on returns); fix underlying type issues instead; use `z.input` (not `z.infer`) for consumer-facing Zod schema types — preserves `.default()` optionality so callers see which fields are truly required
- When asked to explore or investigate, present findings and analysis first; do not jump to code changes until implementation is explicitly requested; dig for the concrete root cause (the smoking gun) — targeted fixes only, not broad investigation plans; for perpetual-loading / stuck-spinner bugs, never route on file extension — content-sniff (BOM + NUL heuristic on first 512 bytes, VS Code `detectEncodingFromBuffer` style) and surface every outcome through a typed discriminated union so the editor render gate covers every state; validate hypotheses directly in source code (e.g., read emscripten/embind internals), no assumptions allowed; assess whether issues stem from incorrect implementation or broken architecture before fixing; never band-aid — fix at source rather than post-processing; verify changes actually work (visually for UI, at runtime for logic) before claiming success; prefer the most correct architectural fix over the simplest — correctness is not negotiable; adversarially review proposed approaches before committing to implementation; question manual additions when automated code generation should handle it; code generators must remain generic at the C++ type level — no domain-specific class patterns, use standard type heuristics (templates, typedefs, inheritance) instead; avoid regex-based assertions in tests bound for upstream PRs — they are hacky and indefensible in review, prefer principled assertions on parsed structures; for cross-pipeline parity (e.g. replicad vs opencascade glTF mesh), assert exact byte-for-byte output via TDD before fixing the underlying cause; also prefer round-trip geometry tests (export → re-import → re-mesh) over byte-count `>0` checks for export pipelines; geometry/test-suite tools must have non-overlapping semantic intent — the LLM should never be forced to choose between two tests that achieve the same thing (e.g. mesh-continuity must measure disconnected-mesh count, not raw mesh count, so adding parts to a unicolor model never spuriously fails the test); geometry tests must derive results purely from the glTF mesh data — never rely on kernel-supplied metadata or glTF `extras` fields (e.g. `assemblyParts` must compute overlaps from geometry alone) so future kernels only need to emit geometry, not extra annotations; chat-tool descriptions follow Claude Code's "When NOT to use:" pattern alongside the primary purpose for context-engineering DX; when removing language that "encouraged the LLM to skip a test" because the geometry unit lacked a top-level export, push the LLM the opposite way — instruct it to add a kernel-specific top-level export so the file becomes testable, never to drop the test; tool descriptions point the LLM at the canonical schema (e.g. `<test_requirements>` for `test_model`) as a single source of truth rather than re-asserting requirement/format guidance in prose — tool-description text stays identical across kernels, with cached tool wrappers differing only via error-path routing (`getKernelConfig`)
- Use `generatePrefixedId(idPrefix.*)` from `@taucad/utils/id` for all ID generation — not `crypto.randomUUID()`; prefixed IDs use nanoid with typed prefixes defined in `@taucad/types/constants` (e.g., `idPrefix.log` = `'log'`)
- When extracting domain logic into standalone packages, invert dependencies — the domain package owns its types/schemas, consumer packages depend on it (not the reverse); derive computed values from canonical sources rather than duplicating data across schemas; for runtime/kernel manifests authored as Zod, convert to JSON Schema during worker-side startup discovery so Zod instances never cross `postMessage` to the main thread; no backwards compatibility or deprecation paths for unreleased/internal APIs — roll forward with breaking changes; do not add shims or adapters for consumers that don't exist yet; kernels must be fully self-contained (no shared cross-kernel schemas/types like `occt-shared-schemas` — each kernel duplicates its schemas to remain independently extractable); appearance/material props (color, opacity, metalness, roughness, density) live as flat top-level keys on shape return values — never nested under an `appearance` key — for parity with Zoo's per-shape `appearance()` and to enable future engineering queries (e.g. stress under load via density); `defineKernel`/`defineTranscoder` author APIs accept Zod schemas only — JSON Schema and defaults are resolved internally so authors don't repeat themselves; schema-driven runtime APIs must use generic inference end-to-end (kernel formats, transcoder edges, `client.export` options) — no `Record<string, unknown>` escape hatches when a schema can drive the type
- JSDoc codeblocks use explicit language tags (`typescript`/`javascript`, not `ts`/`js`); `@public` tag gates compile-checking; `@example` tags require `<caption>` per JSDoc spec (non-empty, no redundant "example" word); examples must reflect actual consumer usage patterns, not synthetic isolated calls; public JSDoc required for `libs/` and `packages/` only — apps are exempt
- Chat composer toolbar, model/kernel selector triggers, and other paired breakpoint UIs: apply the same visible/control changes to both desktop and mobile layouts (including `chat-textarea-desktop.tsx` / `chat-textarea-mobile.tsx` when split) when the feature exists on both

## Learned Workspace Facts

- Policy docs live in `docs/policy/` (testing, library-api, vision, lint, xstate, typescript, filesystem, react-testing, commit, agents-md, context-engineering, jsdoc, diagram, ui, accessibility, engineering-forms, ux, and more); research docs in `docs/research/` (including `rpc-best-practices.md`, `observability-architecture.md`, `grafana-observability-gaps.md`, `occt-js-v8-dx-modernization.md`, `emscripten-idiomatic-js.md`, `occt-wasm-build-comparison.md`, `replicad-occt-v8-opportunities.md`, `observability-metrics-audit.md`, `observability-implementation-status.md`, `embind-smart-pointer-stale-ptr.md`, `embind-return-strategy-benchmarks.md`, `wasm-smart-pointer-landscape.md`, `occt-v8-migration.md`, `ocjs-type-resolution-failures.md`, `ncollection-auto-discovery-build-validation.md`, `parameter-storage-architecture.md`, `embind-js-dispatch-failures.md`, `ocjs-deprecated-symbol-strategy.md`, `ocjs-any-type-analysis.md`, `ocjs-test-failure-resolution.md`, `parameter-middleware-architecture.md`, `knip-dead-code-analysis.md`, `ocjs-cmake-cache-race-condition.md`, `large-repo-import-performance.md`, `vscode-fs-performance.md`, `filesystem-mount-overlay-architecture.md`, `filesystem-mount-only-architecture.md`, `occt-wasm-module-system.md`, `modular-wasm-multithreading.md`, `claude-code-architecture-mining.md`, `ui-startup-performance-gap-analysis.md`, `claude-code-prompting-techniques.md`, `claude-code-skill-injection-techniques.md`, `claude-code-subagent-architecture.md`, `tool-result-display-divergence.md`, `unresolved-dependency-watch-gap.md`, `complex-task-agent-gap-analysis.md`, `agent-skill-patterns.md`, `agent-skill-patterns-v2.md`, `typescript-esm-extension-resolution.md`, `cache-strategy-analysis.md`, `chat-model-cost-forensics.md`, `filesystem-memory-outstanding-items.md`, `geometry-data-transfer-architecture.md`, `geometry-pipeline-copy-audit.md`, `safari-cross-origin-isolation.md`, `agentic-web-search-architecture.md`, `content-aware-watch-filtering.md`, `shared-pool-stale-parameter-reads.md`, `dark-mode-3d-lighting-adaptation.md`, `converter-runtime-consolidation.md`, `schema-driven-export-configuration.md`, `unified-export-pipeline-architecture.md`, `export-pipeline-v2.md`, `parameter-architecture-design-space.md`, `parameter-architecture-v2.md`, `dialog-inline-editing-audit.md`, `file-tree-stale-directory-deletion.md`, `export-pipeline-v3.md`, `export-pipeline-v4.md`, `export-pipeline-v5.md`, `shared-memory-geometry-pipeline.md`, `shared-memory-pool-api-audit.md`, `filesystem-runtime-strategy.md`, `shared-worker-fs-architecture.md`, `shared-worker-gate-startup-performance.md`, `export-pipeline-v5-implementation-audit.md`, `export-pipeline-v6-implementation-audit.md`, `export-option-schema-architecture.md`, `occt-v8-rc5-migration.md`, `replicad-normal-smoothing-pipeline.md`, `replicad-occt-gltf-pipeline-delta.md`, `replicad-occt-normal-pipeline-v3.md`, `brep-edge-mesh-regression.md`, `chat-error-persistence-stale-display.md`, `export-options-kernel-mismatch.md`, `kernel-plugin-type-linkage.md`, `nativehandle-serialization-and-pipeline-architecture.md`, `occt-brep-serialization-for-handle-caching.md`, `step-export-multifile-regression.md`, `type-safe-kernel-ids.md`, `3mf-activation-status.md`, `assimpjs-emscripten-optimization-audit.md`, `generic-inference-pipeline.md`, `lazy-capabilities-manifest.md`, `runtime-client-type-safety-audit.md`, `transcoder-edge-source-type-merging.md`, `image-context-management-gap-analysis.md`, `image-storage-architecture.md`, `polymorphic-js-evaluation.md`, `runtime-require-to-esm-migration.md`, `cli-runtime-ergonomics.md`, `wasm-heap-view-detachment.md`, `3mf-export-scale-orientation-manifold.md`, `tinyusdz-fork-fixes-vs-upstream.md`, `ocjs-additionalcppcode-type-erasure-regression.md`, `ocjs-typescript-codegen-gap-analysis.md`, `define-transcoder-type-safety.md`, `format-map-aggregation-collapse.md`, `replicad-single-dts-type-errors.md`, `capabilities-manifest-api-audit.md`, `openscad-3mf-coordinate-orientation.md`, `import-test-geometry-deviation-audit.md`, `context-summarization-compaction.md`, `multimodal-agent-image-storage-patterns.md`, `agent-loop-safeguards.md`, `concurrent-chat-streams-agents-panel.md`, `mesh-continuity-test-semantics.md`, `netlify-ui-deployment-strategy.md`, `3mf-export-rendering-artifacts.md`, `occt-jsdoc-doxygen-truncation.md`, `assimp-unified-axis-unit-contract.md`); architecture docs in `docs/architecture/` (e.g., `runtime-topology.md`) serve as binding contracts for implementation
- Hybrid oxlint + ESLint linting: oxlint runs first, ESLint handles residual rules; custom Oxlint JS plugins in `libs/oxlint/`; tsgolint (typescript-go) provides type-aware JSDoc codeblock checking via `source_overrides`; rule tests use `oxlint-disable` syntax (ESLint 9 RuleTester strips `eslint-disable` from `getAllComments()`); when only oxlint flags a rule, prefer `oxlint-disable` form — `eslint-disable` for those same names produces "unused directive" warnings since the two linters use divergent rule IDs (e.g. oxlint `no-empty-function` vs ESLint plugin variants); ESLint plugin rules require the plugin prefix in disable directives (e.g. `no-barrel-files/no-barrel-files`, not bare `no-barrel-files`); `oxlint-disable-next-line` only suppresses one line — use block-level `oxlint-disable`/`enable` for sections with multiple violations; tsgolint requires absolute paths — JSDoc codeblock virtualPath must be resolved against the workspace root when `context.filename` is `<input>` (RuleTester default); MDX parser exported separately at `@taucad/oxlint/mdx-parser` (not a property of the ESLint plugin object); `validate-mdx-links` checks internal dead links (relative + absolute, including Fumadocs route groups); `validate-mdx-external-links` checks remote URLs via subprocess with disk cache at `node_modules/.cache/tau-lint/external-links.json`; generated report artifacts under `apps/api/reports/` must be lint-ignored (not hand-written TS)
- External repos in `repos/` managed via `repos.yaml` and `pnpm repos`; gitignored and cursorignored; add to `.oxlintrc.json` ignorePatterns; assimp architectural rule: exporters must NOT carry per-source-format mapping — importers declare units/coordinate-system as scene metadata so each exporter applies transformations to its own spec (GLB is Y-up/meter spec, 3MF is Z-up/mm spec; spec-compliant GLB input is a hard precondition — non-spec-compliant GLB = user/dev error); Tau `@taucad/converter` loaders/exporters do NO geometry computation — all coordinate-system/scaling transforms live inside assimp importers/exporters via scene metadata (`AI_METADATA_UP_AXIS` aligned with FBX convention, accepted as overlap), exporters apply only the minimal transform needed at the spec edge (no import-side post-processing), and the contract is validated via round-trip import→export geometry preservation tests across all format pairs; `repos/opencascade.js` is a fork (`taucad/opencascade.js` from `donalffons/opencascade.js`) and is published to npm as `@taucad/opencascade.js` (3.0.0-beta.0 preview — replicad-specific files are stripped before `npm pack`); OCJS tests are TS-only (pytest dropped — 30-min build acceptable to keep parity with all other OCJS tests); `repos/assimpjs` is a fork (`taucad/assimpjs` from `kovacsv/assimpjs`) published to npm as `@taucad/assimpjs` and consumed via pnpm alias (`assimpjs: npm:@taucad/assimpjs@<version>`); `repos/assimpjs/assimp` is a fork (`taucad/assimp` from `assimp/assimp`, upstream branch `master`); `repos/assimpjs/assimp/contrib/tinyusdz` tracks `taucad/tinyusdz` from `lighttransport/tinyusdz` (upstream branch `release`, not `main`); tinyusdz upstream PR target is `dev` (not `release`) — all CI workflows (`linux_ci.yml`, `macos_ci.yml`, `windows_ci.yml`, `codeql-analysis.yml`) only trigger on `pull_request: branches: [ dev ]`; tinyusdz is C++14, **no C++ exceptions** (uses `nonstd::expected` instead), dependency-free (no codegen), Google clang-format 2-space indent (run `clang-format -i` before commit); every PR auto-reviewed by upstream `claude-code-review` bot — disclose AI co-authorship explicitly in PR body; USDZ output validated via `usdchecker --arkit` (Apple USD Tools) — Sdr-compliance violations like `MismatchedPropertyType` for `UsdUVTexture::st` (expected `float2`, got `texcoord2f`) reproduce against synthetic minimal USDA fixtures; pxrUSD's own `usdcat` canonicalises booleans as `1`/`0` (not `true`/`false`) — both forms are spec-valid; lib3mf v2.4.1 integrated via `Lib3MFBridge` for 3MF import/export (3MF is now a fully-wired native export option — the prior `export3mf` runtime util was removed in favor of the unified converter transcoder pipeline); three WASM variants: mini (import-only), all (full import+lib3mf), exporter (export+lib3mf); assimpjs `ConvertFileList` accepts optional `options` third param for per-format export options (e.g., 3MF units, slicer metadata); silencing assimp/tinyusdz debug logs (e.g. tinyusdz USD exporter chatter visible from CLI exports) requires editing C++ sources, rebuilding the WASM variant, and relinking via the `dist→replicad-opencascadejs/src → npm pack → tarball install` flow — runtime suppression is not possible post-build; `-O4` wasm-opt pass applied to exporter WASM variant for size optimization; 3MF axis/unit transforms propagated via assimp `ExportProperties` API (`3MF_EXPORT_UPAXIS`, `3MF_EXPORT_UNIT`) read directly by `Lib3MFBridge.cpp` — distinct mechanism from scene-metadata-driven exporters (`gltf`, `fbx`, `dae`, `obj`, `ply`, `stl`, `3ds`, `x`, `x3d`, `usda`, `usdz`) which use `bakeContractTransformIntoMeshes` to apply `AI_METADATA_UP_AXIS` + `AI_METADATA_UNIT_SCALE_TO_METERS` at the spec edge; `step`, `m3d`, `pbrt`, `assbin`, `assxml`, `assjson` have no axis-transform support; `@taucad/converter` `exportFromGlb(glbData, outputFormat, exportProperties?, sceneMetadata?)` forwards both per-format ExportProperties and aiScene metadata into `assimpjs.ConvertFileList`; `repos/emscripten` is a shallow reference clone (not a fork — no `fork:` field in `repos.yaml`); per-build emsdk scoping: assimpjs WASM uses `repos/assimpjs/emsdk`, OCJS/replicad WASM uses `repos/opencascade.js/deps/emsdk` — never cross-mount these (build flag/env mismatches break `__cpp_exception` linkage); `repos/opencascade.js` WASM build uses `BUILTIN_ADDITIONAL_BIND_CODE` (Python layer), full builds (10-30+ min) use `nohup`; `bindings.py` has `_classify_js_type`/`_build_dispatch_tree`/`_codegen_dispatch_tree` for val-based constructor/method overload dispatch, `processEnum` uses `enum_value_type::string` (string enums); `bindings.py` `needsWrapper` only wraps builtInTypes/enums/C-string `T&` via `getReferenceValue`/`updateReferenceValue` + `emscripten::val` (`{current: value}` pattern); `Handle<T>&` output params use unified return-by-value via `emscripten::value_object` (returns properties as JS object instead of mutating `{current}` refs); OCCT convention: `const handle<T>&` = input, non-const `handle<T>&` = output (never bidirectional); `const` on method guarantees `T&` params are output-only; suffix-free symbol generation removes `_N` overload subclasses; `shouldStripParam` unconditionally strips primitive output params (`double&`, `int&`) from JS signatures — callers no longer pass ceremonial `0` args; NCollection auto-discovery generates `using` declarations replacing deprecated `_SAFE_DEPRECATED_PREFIXES`; `bindings.py` emits explicit type defs for runtime methods requested via `sEXPORTED_RUNTIME_METHODS` (e.g. `HEAP32`, `HEAPU8`, `HEAPF32`) so consumers gain full type safety on direct WASM heap reads; namespace wrapping in generated `.d.ts` removed — fix type errors in the python codegen layer rather than adopting `--emit-tsd`; `dts-validation.test.ts` tracks `.d.ts` `any` count to prevent type resolution regressions; build system uses `build-cache.py` keyed on `OCJS_EXCEPTIONS`/`OCJS_SIMD` + other env vars; when build flags change, PCH must be rebuilt (`./build-wasm.sh pch`) and stale cache entries cleared from `repos/opencascade.js/cache/`; `OCJS_EXCEPTIONS=1` enables `-fwasm-exceptions` (Node.js supports `WebAssembly.Exception` — the real constraint is all `.o` files AND the linker must consistently use `-fwasm-exceptions`, otherwise `__cpp_exception` becomes an unresolved import); `OCJS_SIMD=1` enables `-msimd128 -mrelaxed-simd`; `OCJS_BIGINT=1` enables `-sWASM_BIGINT=1`; `OCJS_EVAL_CTORS_LEVEL` controls `-sEVAL_CTORS=N`; `build-flags.json` manifest validates compile-time flag consistency across cached stages; `build-manifest.json` stores symbol validation results (compiled count, missing symbols, WASM/JS sizes); `build-wasm.sh` uses MD5 hash-based patch freshness detection; `DEPS.json` `commit` field enables stamp-based dep version checks (doxygen, consistent with occt/rapidjson/freetype); WASM experiments defined in `scripts/experiments/*.yml`, orchestrated by `scripts/wasm-experiment.sh`, artifacts stored in `tarballs/experiments/` with `provenance.json` metadata; benchmark CLI `--wasm-variant single-exceptions` maps to `replicad_with_exceptions.*`, `--wasm-variant single` maps to `replicad_single.*`; both root `package.json` and `packages/runtime/package.json` must reference consistent replicad/replicad-opencascadejs tarballs — version mismatch causes `UMin` runtime errors; NX `build` target depends on `validate` (runs `validate-build.py` → `<variant>.build-manifest.json`) and `provenance` (runs `provenance.py init` + `finalize` → `<variant>.provenance.json`); use `pnpm nx build ocjs` to get both artifacts in one pass (separate `validate`/`provenance` invocations cause NX `link` cache to restore `dist/`, wiping prior step's output); replicad WASM single-variant v8 config at `repos/replicad/packages/replicad-opencascadejs/build-config/custom_build_single_v8.yml`; WASM rebuild integration into Tau: copy dist→replicad-opencascadejs/src, `npm pack`, move tarball to `tarballs/`, `pnpm install`, copy-assets via `copy-files-from-to.cjson`, then run `packages/runtime` tests; `pnpm patch` failure mode: stale `node_modules/.pnpm/*patch_hash*` directories can persist with intact `patch_hash` markers but unmodified file contents after a botched install — symptom is cascading TS2835 errors (e.g. `@taucad/kcl-wasm-lib`, `@langchain/openai`) from missing patched type declarations; recover by removing the stale `*patch_hash*` dirs and re-running `pnpm install` so the patch is re-applied; replicad PBR property is named `metalness` (not `metallic`) — `repos/replicad/packages/replicad/src/export/assemblyExporter.ts` was renamed in v8.56 to align with `ShapeConfig.metalness` consumed by the Tau replicad kernel; after the WASM/source rebuild, regenerate Monaco typings via `pnpm nx extract-replicad api-extractor` so `libs/api-extractor/src/generated/replicad/*` reflects the renamed field; `repos/ForgeCAD` from `KoStard/ForgeCAD` (fork `rifont/ForgeCAD`) for AI-native CAD exploration; `repos.yaml` supports optional `commit` field for pinning repos to specific commit hashes (`pnpm repos add -c <hash>`, `pnpm repos sync` honors pinned commits); `repos/vscode` used as architectural reference for FS events, IPC, caching, and binary transfer patterns
- UI deployed to Netlify (`apps/ui/netlify.toml`); branch UI deploys to Netlify for every branch off `main`, staging UI auto-deploys via existing Netlify CI/CD, production UI is triggered manually from `.github/workflows/ci.yml` alongside the Fly.io API deploy (parallels Novu's reusable-dashboard-deploy workflow pattern); `netlify.toml` env vars are build-time only — not available in SSR functions, so derive runtime values in `environment.config.ts` preprocess; `NX_PREFER_NODE_STRIP_TYPES=true` must be inlined in build commands (Nx evaluates at module load, before `.env` files); `apps/ui` requires `tslib` as a runtime dependency (not devDep) because `react-router-serve` runs the built bundle in production (Fly.io UI image start failure mode); per-project `build.outputs` overrides in `project.json` that point to non-existent paths silently break NX cache restore (manifests as `size-limit` "can't find files" failures) — rely on the inherited `outputs: ["{projectRoot}/dist"]` from `tools/tsdown.plugin.ts` rather than hand-rolled overrides; PostHog analytics loaded lazily via `posthog.lib.ts` — static import blocks main thread ~2.5s during startup; Shiki syntax highlighting grammars loaded on-demand (not eagerly imported); `@taucad/converter` exports separate entry points: `@taucad/converter/formats` provides lightweight format metadata (extensions, MIME types) without importing full conversion/import/export dependencies; `mimeTypes` in `mime-types.constants.ts` is the single source of truth for 3D file extensions — `fileExtensions`/`fileExtensionSet`/`createExportFile` derived from it; `ExportFormat` type removed — use `FileExtension`; converter `importFormatKeys` derived from `fileExtensions` with `unsupportedImports` exclusion set; `packages/converter` `import.test.ts` baselines round-trip assertions on `standardCubeGeometry` via `createCubeTestCase('<fmt>', { variant, geometry: standardCubeGeometry })` — per-format deviations from the gold-standard shape indicate incomplete assimp normalization (importer/exporter transform gap or fixture mismatch) rather than legitimate format differences and warrant investigation; cross-origin isolation uses `COEP: require-corp` (not `credentialless`) for universal browser support including Safari — all external resources are same-origin proxied (PostHog via `/api/ph`, GitHub avatars via `/api/github-avatar`), so `require-corp` works without breakage
- `packages/runtime` is consumed as source via package.json exports, not built output; test mocks in `packages/runtime/src/testing/kernel-testing.utils.ts`; `createRuntimeClientOptions` merges options via `deepmerge` — plugin arrays match by `id` and replace, non-array fields are deeply merged; Vite plugins in `@taucad/vite` with `*.vite-plugin.ts` suffix, `vite:` prefix, Vite 8 hook filters; gitignored `src/**/wasm/` dirs populated by `copy-assets` target via `copy-files-from-to.cjson`; `parameterFileResolverMiddleware` merges per-geometry-unit parameter file overrides from `.tau/parameters/<entry>.json` with code-level defaults; two parameter sources: (1) `getParameters` extracts schema/defaults from source code, (2) `.tau/parameters/<entry>.json` stores user overrides organized as named parameter groups with `activeGroup` selection; `FileParameterEntry` = `{ activeGroup, groups }`, `ParameterGroup` = `{ values }`; project machine manages groups via `switchParameterGroup`, `createParameterGroup`, `deleteParameterGroup`, `renameParameterGroup` events; context uses `parameterEntries: Map<string, FileParameterEntry>` keyed by entry file path; kernel worker watches per-geometry-unit parameter file via `registerWatchPath` for auto-re-render; middleware `getDependencies` hook returns file paths to include in the dependency hash — parameter file resolver uses it so per-geometry-unit parameter file changes invalidate the geometry cache; `BundleResult.unresolvedPaths` tracks import paths that failed to resolve (ENOENT) during bundling — kernel worker watches these paths so creating missing files triggers re-render recovery; runtime protocol includes `'buffering'` worker state (`idle → buffering → rendering`) so debounce windows are visible to UI consumers — runtime worker is the authoritative source of truth for worker state; `@taucad/runtime/testing` requires `// @vitest-environment node` in UI tests (esbuild-wasm incompatible with jsdom); `Tessellation.angularTolerance` is in degrees — replicad/OCCT APIs expect radians, convert with `* (Math.PI / 180)` at the boundary (same pattern as OpenCascade kernel); replicad mesh uses single-WASM-call C++ extractor (`extractMeshData`) with SLProps-based surface-derived normals — byte-for-byte parity with OCCT's glTF writer; replaces prior JS-side `ComputeNormals()` approach; `cad-preview.machine.ts` passes parameters directly to kernel (not via filesystem middleware); render timeout: `setRenderTimeout` protocol command, `KernelWorker.executeRender` uses `Promise.race` with configurable timeout, `RenderTimeoutError`/`isRenderTimeoutError` guard; public API uses seconds (matching UI dropdown), internal protocol uses milliseconds; default `defaultRenderTimeoutMs = 30_000`; `fromNodeFs` provides Node.js filesystem integration for headless/CLI usage; `execute-code-node.ts` evaluates user code in Node.js via `vm.Module` (polymorphic with browser's data-URL worker approach); runtime is ESM-only (no `require`) — env-detected branches use `taucad:` prefix constants for module identifiers; package version injected at runtime via `package-info.ts` (env-aware: bundler define for browser/worker, dynamic read for Node) so GLB/glTF metadata embeds the exact `@taucad/runtime` version cross-environment; WASM heap detachment ("Cannot perform %TypedArray%.prototype.slice on detached or out-of-bounds ArrayBuffer") is mitigated by copying TypedArrays at the JS boundary — `GROWABLE_ARRAYBUFFERS` (`wasmMemory.toResizableBuffer`) is still experimental in Node 24 and requires a runtime flag, so the copy approach remains the correct architectural choice; runtime geometry two-layer transport: byte array (`Uint8Array`) and shared memory (`SharedPool`-backed `SharedArrayBuffer`); domain-driven SAB ownership: FM machine allocates file pool SAB (`filePoolBuffer`), RuntimeClient allocates geometry pool SAB — `cad.machine` reads `snapshot.context.filePoolBuffer` from FM and passes via `connect({ port, filePoolBuffer })`; `client.geometryPool` is the only pool on the public `RuntimeClient` interface (no `client.filePool`/`filePoolBuffer`); `sharedMemory` options only configure `geometry`; `GltfContentDelivery` pooled variant is `{ delivery: 'pooled', key }` (no `pool` field); SharedPool population is a framework concern — the dispatcher's `toTransportGeometry()` auto-stores geometry before checking for pooled delivery (store-then-check with inline fallback), not middleware; `RuntimeClient.resolveGeometry()` encapsulates SAB→ArrayBuffer resolution so consumers are transfer-mode agnostic; browser `TextDecoder` rejects `SharedArrayBuffer`-backed views — copy at consumer boundary required; `LruMap` in `libs/utils` provides in-memory LRU caching for geometry-cache and parameter-cache middleware (L1 memory + L2 filesystem); each kernel has a `*.plugin.ts` file co-locating extensions, detect patterns, builtinModuleNames, and `new URL` worker reference; `canHandle` removed from kernel protocol — plugin extensions are the sole file-detection mechanism; `kernel-factories.ts` is a pure ESModule dynamic-import factory; transcoders are injectable format converters (bytes→bytes) following the same co-located `*.plugin.ts` pattern as kernels; `defineTranscoder` mirrors `defineKernel` — declarative `edges: TranscoderEdge[]` (replaces `discoverEdges`/`canTranscode`) drives type-safe `input.to` inference; `transcoder-factories.ts` barrel; `TranscoderEdge` declares conversion capabilities (from/to/fidelity); converter transcoder wraps `@taucad/converter`; transcoders live in `packages/runtime/src/transcoders/`; export option schema resolution: each kernel plugin defines self-contained per-format export Zod schemas via a single `exportSchemas` map (replaced separate `exportFormats`+`exportOptionSchemas` — keys define the supported format list); tessellation is per-kernel, not global — removed from `CreateGeometryInput`; render and export pipelines share the same per-kernel schema with different defaults; JSON Schema-first merging during plugin resolution; resolved manifest exposes complete JSON Schema for all consumers; OpenSCAD uses native `$fn`/`$fa`/`$fs` tessellation params (not OCCT linear/angular tolerances) — render path merges user-defined + default params with user values taking precedence, export path always overrides with UI-supplied values; OpenSCAD uses 2-stage parameter extraction — extract all params first (including tessellation `$fn`/`$fa`/`$fs`), then filter UI-facing params before exposing to consumers, so user-defined tessellation values can be detected and merged with kernel defaults without a separate scan; default coordinate system convention is Z-up across all formats (replicad/opencascade kernels are Z-up natively); OpenSCAD kernel emits glTF Y-up natively (deviates from the Z-up convention) so transcoded 3MF/assimp exports must propagate `coordinateSystem` through to assimp's per-exporter axis-transform flag rather than relying on the kernel's GLB orientation; transcoded format exports merge intermediate-hop options (e.g. Replicad→GLTF→USDZ retains GLTF tessellation/coordinateSystem); `client.export` is fully type-safe via generic inference that merges native-kernel-format schemas with downstream transcoder schemas; protocol uses `activeKernelChanged` events with a discriminated `KernelId` union (not raw string) for kernel-aware UI/export route selection; OCCT `BRepMesh_IncrementalMesh` caches triangulation on `TopoDS_Shape` — pass `decreate=true` to force re-mesh when tessellation tolerance decreases (otherwise stale fine mesh is returned); `defineKernel` exposes a single `options` field on both `createGeometry` and `exportGeometry` (renamed from `renderOptions` for symmetry) and the value is always required (defaults to `{}` when no schema is declared); native-handle caching uses `serializeHandle`/`deserializeHandle` on `defineKernel` with the serialize return type captured as a generic so deserialize is fully type-safe — `ensureNativeHandle` is an internal runtime middleware capability (not a `defineKernel` API) that re-hydrates handles via the geometry-cache middleware so kernels don't have to re-implement preheating; `@taucad/json-schema` package owns Draft-07 JSON Schema TS typings — fully self-contained, no external `json-schema` npm types dep; `CapabilitiesManifest` shape: collapsed parallel arrays into a single `routes` array (no `routeId`), fidelity lifted onto kernel definitions, helpers `routesFor`/`bestRouteFor` exposed on `RuntimeClient`, manifest replays current value to late subscribers; JSON Schema-first merging chosen over Zod-first for runtime resolution while preserving Zod authoring DX (`z.object({})` retained — problematic generated types filtered out instead of switching to `z.unknown`)
- Socket.IO RPC uses `emitWithAck` pattern (replaced manual `pendingRequests` Map); prod uses `RedisIoAdapter` with `@socket.io/redis-streams-adapter` for horizontal scaling and CSR; dev uses standalone Socket.IO server on `PORT+1` via `DevWebSocketService` without Redis adapter; auth via `server.use()` middleware in gateway `afterInit`; health probes at `/health/live|ready|startup` via `@nestjs/terminus` with custom `RedisHealthIndicator` and `DatabaseHealthIndicator`
- API deployed to Fly.io (`apps/api/fly.prod.toml`); OTEL SDK bootstraps in `apps/api/app/telemetry/otel.ts`; metrics derived from `@taucad/telemetry` canonical definitions; `PrometheusExporter` on port 9464; traces+logs via OTLP/HTTP to Grafana Cloud; `TelemetryModule` (`@Global`) provides `TracerService` + `@Span()` decorator (only approved tracing approach; `withSpan` removed); `prom-client` fully replaced by `@opentelemetry/api`; Grafana dashboards-as-code in `infra/grafana/` with 10 dashboard JSONs + alert rules, synced to Fly.io Grafana via `apps/api/scripts/sync-grafana-dashboards.sh`; `grafana/otel-lgtm` in `docker-compose.yml` provides local LGTM stack parity with named volumes for data persistence
- Model benchmark suite at `apps/api/app/benchmarks/` — runner, geometry grader, HTML report generator for evaluating AI model performance on CAD tasks; uses OpenSCAD kernel for geometry validation; benchmark runner sends `kernel` metadata in chat messages which controls system prompt generation — must match target kernel; CLI at `apps/api/scripts/benchmark-models.mts` requires `node -r dotenv/config --import @oxc-node/core/register --import ./scripts/raw-loader.mts`; outputs JSON + HTML reports with geometry validation, GLB artifacts, and generated source code; `test.json` is per-file shape `{ [sourcePath]: { requirements: [] } }` so a project tests multiple geometry units concurrently — `targetFile` is required (no `?? mainEntryFile` fallback) on every agent-surface RPC and tool input (`fetch_geometry`, `capture_screenshot`, `test_model`, `edit_tests`, `get_kernel_result`); per-geometry-unit geometry artifacts use `${toolCallId}__${slug(targetFile)}.glb`; reasoning timing is stateless server-side via `providerMetadata.common.reasoningStartedAtMs` on each `reasoning-start` part keyed by reasoning-block `id` (correlates interleaved thinking blocks across one message), surfaced via `MyUIMessage` typed schema — UI renders 'Thought briefly' (<1s) or 'Thought for Xs' from start/end timestamp diff (no client stopwatch, no clock-skew compensation); `agent-safeguards.middleware.ts` (`apps/api/app/api/chat/middleware/`) detects repeated identical tool failures (3 consecutive errors on the same tool+input) and injects a one-shot guidance message into the latest tool result steering the LLM to alternative actions — appended to the existing tool result so it does not invalidate Anthropic prompt cache; full system-prompt audit at `docs/research/system-prompt-audit.md` follows the gap-analysis checklist style of `filesystem-gap-analysis.md`; system prompts and tool-call descriptions use "geometry unit" everywhere — the "geometry unit" acronym is reserved for internal code only; kernel-specific test-failure recovery guidance lives inside each kernel's section of the system prompt (never as unconditional generic text that would mislead when paired with another kernel — e.g. don't reference OpenSCAD entry-point conventions in the Replicad section); `test_model` dynamically creates the geometry unit on demand rather than asking the LLM to bootstrap — missing-file errors stay legitimate failures, not pre-bootstrapping requests; testing-related prompts push the LLM to add a top-level kernel-specific export so a file becomes testable rather than to skip writing the test
- Editor architecture: machine owns openFiles, ref-counting, force-close; dockview subscribes only; use unique panel IDs (not file path); single FS worker with provider-based backends (`AbstractProvider` → `DirectIdbProvider`, `FsAccessProvider`, `OpfsProvider`, `MemoryProvider`); `ProviderRegistry` is a pure factory (no active backend state or provider cache); `MountTable` for multi-backend mount points — `mount(prefix, provider, config)` with `MountConfig = { backend, ...options }`; `FileService.mount(prefix, backend)` / `unmount(prefix)` is domain-agnostic — callers compose domain paths (e.g., `/projects/${id}`), FileService never knows about projects; `FileService.reconfigure()` removed — mount/unmount is the only configuration surface; `CrossTabCoordinator` for multi-tab sync; `SharedPool`/`SharedMemoryArena` in `packages/memory` for zero-copy content sharing; FM machine allocates file `SharedArrayBuffer` and posts to FM worker + creates `SharedPool` for `FileContentService`; `FilePool` type (was `ContentPool`) in `@taucad/runtime/filesystem`; `FilesystemObserverBridge` wraps native `FileSystemObserver` API; ZenFS dependency removed — direct IndexedDB/OPFS/FS Access API implementations replace it; `packages/filesystem` large-repo layer: `InMemoryFileTree` (O(1) stat/readdir), `ResourceWriteQueue` (per-parent-dir writes), dedicated import worker for off-thread fetch+extract+write; `fromSafeAsync` (`#lib/xstate.lib.js`) replaces `fromPromise` for all UI XState async actors; `chat-editor-dockview.tsx` is the active editor; file-type rendering uses viewer registry pattern; two filesystem watch planes: kernel fast path (dependency-scoped) and UI tree path (directory-scoped) — do not merge into one coarse stream; chat activity folding: `assistant-message-activity.ts` groups tool/reasoning parts into Cursor-style collapsible sections (`chat-activity-group.tsx`, `chat-activity-section.tsx`, `chat-activity-fold-context.tsx`); groups stay open during streaming and auto-close on completion; user toggle overrides persist; tool rows within groups default collapsed and use full-width row triggers (entire row toggles open) while keeping the chevron inline next to the label (not pushed to the right edge); all chat-tool cards (`chat-message-tool-*`, activity sections, etc.) render their `<verb> <description>` text via the shared `chat-tool-label.tsx` component — no per-tool styling, single point of adjustment to keep spacing/coloring consistent across call sites; chat-tool labels follow a unified two-tone hover palette (muted verb + foreground description on hover, mirroring `chat-message-reasoning.tsx` "Thought") — `viewer-link.tsx`/`file-link.tsx` share the same hover ramp; never use green/orange/red text on chat parts (color is reserved for the leading icon only; trailing chevrons follow the muted/foreground palette, never a status color); chat-tool icons render with NO color in success states — only red icons indicate failure (success doesn't draw attention); all `ChatToolError` rows follow Pattern A copy "Failed to <verb>" (e.g. "Failed to compile", not "Compilation failed/error"); standardized chat-tool verbs unify across grep/glob/web-search as "Searched", web browser as "Visited"/"Visiting pages", `test_model` success as "Tested X requirements", compaction as "Summarized chat context", unknown-part fallback as "Received unknown part <part-type>"; `chat-tool-error.tsx` is a Radix `Collapsible` with the error body inside the collapsible content — same UX as `chat-message-reasoning`/`chat-message-tool-get-kernel-result` errors; pane-header actions (`paneview-header.tsx`, `chat-parameters.tsx`) render on hover only across all panes — no special "main geometry unit" exemption — and the opacity transition lives on the parent action-group container so children inherit one animation rather than each declaring its own; the parameter group selector is hover-gated and renders on every geometry unit pane (not main-only); group summaries expose both `summaryVerb` (past tense, e.g. "Explored") and `summaryVerbActive` (present participle, e.g. "Exploring") with the active form rendered only while streaming/chat is active (cancelled or completed flips back to past tense); active group titles render with the same `text-shimmer` effect used by ongoing tool rows like `chat-message-tool-read-file.tsx` — shimmer + present-tense titles are scoped to the trailing message only, so previous messages always show past tense even while chat is still streaming a later message; exploration phase summary aggregates kernel verification ("X renders"), screenshot capture ("X images"), `test_model` ("X tests"), file reads, web/code searches (merged into a single "X searches" item), and grep/list calls; reasoning appears inside groups but never in group titles; file/CAD mutation operations are never grouped (always separate); no nested groups — bridged groups flatten inner contents; the latest in-flight group remains visually ungrouped (parts shown inline) until the next part arrives that signals end-of-group; binary-file editing pipeline: `FileContentService.resolve()` returns a discriminated `FileContentResult` (`loading | text | binary | too-large | orphaned | error`) sniffed via `apps/ui/app/lib/seems-binary.ts` (BOM + NUL heuristic on first 512 bytes) — extension-based `binaryExtensions`/`isBinaryFile` removed; `BoundedFileCache.set()` returns boolean and never silently drops oversized files (was the perpetual-loading smoking gun); byte-only consumers call `resolveBytes()` which throws typed errors (`BinaryFileError`, etc.); `chat-message-reasoning.tsx` uses sticky-bottom auto-scroll (pin while streaming, pause when user scrolls up, resume when user reaches bottom again); components depending on `ChatToolCard` (whose `useCookie` calls `useRouteLoaderData`) need a router stub or `vi.mock('#components/chat/chat-tool-card', …)` in tests — pattern from `chat-message-reasoning.test.tsx`; model brand icons in `chat-message-data-usage.tsx` resolve via `<SvgIcon id={model.details.family} />` for parity with `chat-model-selector.tsx` (e.g. Gemini → Google icon, not VertexAI provider icon — provider routing is selector-only, not visual identity); chat textarea uses TipTap for rich text editing with `@-reference` chip parsing (context suggestion, slash commands, chip node rendering via `apps/ui/app/components/chat/tiptap/`); `@-reference` chips round-trip via `@path` serialization — shared `ContextChip` component (`onRemove`/`interactive` props) for visual consistency, `rehype-at-references` uses `<mark>` elements (not `<span>`) with `AtReferenceContext` for O(1) file/chat validation; slash command chips (`/command`) share the same parse/rehydrate/render pipeline as `@path` references; chat panels use Dockview `PaneviewReact` for multi-geometry-unit grouping; `chat-kernel` decomposed into `chat-kernel-*` sub-files; settings dialog uses `SettingsSection` union with `SettingsGroup` dividers — Models and Agents are settings tabs (not standalone routes); `modal={false}` on Radix DropdownMenus in chat UI to avoid O(n) DOM traversals with large DOMs; `ClientOnly` component accepts `fallback` prop for layout-stable skeleton rendering during SSR/hydration; `chat-textarea-skeleton.tsx` prevents layout shift; `HeroViewerGate` defers Three.js/CAD viewer loading on landing page behind `requestIdleCallback`; file-manager machine two-phase init: `connectingWorker` (fast: worker+bridge+proxy) → `initializingServices` (slow: backend+mount+readDir+services) → `ready`; `SharedWorkerGate` opens after first phase sets `context.worker`; `ThrottledWorker` in `packages/filesystem` for chunked FS event delivery (100/chunk, 200ms delay, 10k buffer) between `EventCoalescer` and port emission; scroll shadow utilities (`scroll-shadows-x`/`-y`) use `mask-image` + `animation-timeline: scroll()` with `@property initial-value: black` — no `--scroll-shadows-bg` variable needed, works on any background; `bg-*` must be on a parent/wrapper, not on the same element as `scroll-shadows-*` — placing both together makes the mask fade the background too, revealing a mismatched color underneath; `paneviewStyleOverrides` sets `--dv-paneview-active-outline-color:transparent` to suppress focus outlines; dockview sash cursors overridden to `col-resize`/`row-resize`; dockview CSS rules (outside `@layer`) outrank Tailwind v4 utilities — override with `!important` suffix (e.g., `[&_.dv-pane-body]:overflow-y-hidden!`); `parameters-number-field.tsx` is the unified Blender-pattern slider-input component — single field where the slider track IS the input, replacing the separate `ParametersNumber` slider + `ParametersInputNumber` pair; follows `docs/policy/engineering-forms-policy.md` layout rules (45% label width, right-aligned numerics, container-query responsive collapse); `PaneviewHeaderTitle` uses HTML `dir='rtl'` attribute for left-side path truncation (CSS `direction` property alone does not trigger `text-overflow` truncation correctly); route `handle.actions()` renders project-scoped header actions (e.g. Export button via `project-export-action.tsx`) between `AlphaBadge` and `Commands` in the page header; `chat-parameters.tsx` exposes a context menu (dots-on-hover + right-click, mirroring `chat-file-tree.tsx`) with "close geometry unit" and "Quick export" — Quick export opens an `ExportSelector` (`apps/ui/app/components/files/export-selector.tsx`, analogous to `file-selector.tsx`) listing all export formats per geometry unit using the `chat-converter` best-path logic; closing a geometry unit surfaces a centered "Reopen renderer" overlay button in `chat-viewer.tsx` so the GLB can be restored (close is non-destructive, never blow away the geometry); the parameter pane context menu also exposes "Open in viewer" (via `viewer-link.tsx`) and "Open in editor" alongside close/Quick-export; `viewer-link.tsx` (`apps/ui/app/components/files/viewer-link.tsx`) opens a file in a new viewer tab with a tooltip — analogue of `file-link.tsx` for the viewer rather than the editor — used by tool artifact links (e.g. `test_model` GLB outputs); `SvgIcon` `id` is strongly typed as a closed icon-key union (not raw `string`/empty); `useModels` (`apps/ui/app/hooks/use-models.tsx`) centralises model fallbacks via the same map-based lookup pattern as `use-kernel.tsx` — unknown model families resolve to an `'unknown'` family key which `SvgIcon` renders as nothing, replacing scattered anthropic-logo defaults at callsites; `chat-message-tool-edit-file.tsx` detects no-op edits (0 lines added / 0 lines removed by the underlying patch) and renders the simpler inline `chat-tool-file-operation.tsx` style with copy "Edit attempted, no changes" rather than the full collapsible card — empty/confusing collapsibles for zero-diff edits are a UX smoking-gun signal; `chat-message-tool-test-model.tsx` collapses success and failure into ONE render path — single `ChatToolCard variant='minimal'` with identical header chrome, no nested cards on failure, the only failure cue is `ChatToolCardIcon tone='destructive'` and `isDefaultOpen={hasFailures}` to auto-expand failures (per-file `RequirementIndicator`s nested inside `[data-target-file]` groups; no header-level aggregate); `viewer-link.tsx` always renders an inline lucide `Box` cube icon to the LEFT of the filename to visually differentiate it from `file-link.tsx` (which uses extension-based icons) — inline-icon vertical centering uses `align-middle relative -top-px` (NOT `align-text-bottom`, which sags below baseline; NOT negative margin, which shifts the line box) so the icon optically centers against lowercase text without altering line-box height; all chat-tool cards (and tool rows rendered inside activity groups) must constrain to the `chat-history.tsx` virtuoso scroller width — never introduce horizontal overflow or an x-scroll bar at the chat-history level (regressions usually come from inline tool components forgetting `min-w-0`/parent-width clamping when migrated out of the card wrapper); chat-tool title-row ellipsification is a single-line truncation contract enforced by `apps/ui/app/components/chat/title-row-truncation-contract.test.tsx` — `file-link.tsx`/`viewer-link.tsx` rendered inside a chat-tool label must be `display:inline` (not block) and inherit the parent's `min-w-0` truncation context so the verb + filename collapse uniformly as one string ("Failed to compile main..."), never letting the link establish its own intrinsic-content min-width that protects the filename while truncating only the verb; concurrent chat-stream architecture splits draft ownership from streaming ownership: `<ChatInstanceRegistry>` (`apps/ui/app/hooks/chat-instance-registry.ts`) is an in-memory registry of live `<ChatInstance>` mounts owned by `<ChatRegistryProvider>` (`apps/ui/app/hooks/chat-registry-provider.tsx`) at the app shell with per-chatId scoped subscriptions and stable snapshot refs (no `useSyncExternalStore` tearing); `<ChatInstance>` (`apps/ui/app/hooks/chat-instance.tsx`) owns `useChat` + `chatPersistenceMachine` + streaming/RPC and self-registers; `<ActiveChatProvider>` (`apps/ui/app/hooks/active-chat-provider.tsx`) is a per-subtree binding to the currently active chat for chat-input UIs (composer, message editor) and owns only the `draftMachine` in real (`chatId`) or ephemeral (`undefined`) mode — marketing/homepage routes pass `undefined` to skip IndexedDB writes; `<GlobalChatFlushGuard>` (`apps/ui/app/components/global-chat-flush-guard.tsx`) is a single app-shell flush guard that fans out `flushNow` to every registered chat's persistence + draft actors on page unload, replacing per-route `FlushOnCloseGuard`/`HomepageChatFlushOnCloseGuard`; one `ChatRegistryProvider` mounted near `root.tsx` works for every route (project, homepage, marketing) — no forked provider for non-project routes
- Monaco IntelliSense types: `libs/api-extractor` generates bundled `.d.ts` per kernel; `TypeAcquisitionService` registers them via `addExtraLib` at `file:///node_modules/<pkg>/index.d.ts`; custom declarations for opencascade.js live in `repos/opencascade.js/src/declarations/`
- Vite 8 `vite:oxc` plugin resolves `@oxc-project/runtime` helpers; CJS helpers evaluated in SSR ESM runner cause `module is not defined` — mitigated by `oxcRuntimeEsmPlugin` in `libs/vite` (`@taucad/vite`), with its own tests
- Typechecking uses `tsgo` (Go-based TS compiler); do not add cross-project `references` arrays to `tsconfig.json` (causes TS6305); `tsgo --incremental` writes `out-tsc/` directories with `.tsbuildinfo` even under `--noEmit`, and emitted stale `.js` files can be picked up by vitest's default test discovery — exclude `out-tsc/**` from vitest config (these dirs are gitignored stale build output, safe to delete); avoid `using`/`await using` in shipped code — Rolldown won't downlevel and Safari lacks support; use try/finally with `[Symbol.asyncDispose]()` instead

<!-- nx configuration start-->
<!-- Leave the start & end comments to automatically receive updates. -->

## General Guidelines for working with Nx

- For navigating/exploring the workspace, invoke the `nx-workspace` skill first - it has patterns for querying projects, targets, and dependencies
- When running tasks (for example build, lint, test, e2e, etc.), always prefer running the task through `nx` (i.e. `nx run`, `nx run-many`, `nx affected`) instead of using the underlying tooling directly
- Prefix nx commands with the workspace's package manager (e.g., `pnpm nx build`, `npm exec nx test`) - avoids using globally installed CLI
- You have access to the Nx MCP server and its tools, use them to help the user
- For Nx plugin best practices, check `node_modules/@nx/<plugin>/PLUGIN.md`. Not all plugins have this file - proceed without it if unavailable.
- NEVER guess CLI flags - always check nx_docs or `--help` first when unsure

## Scaffolding & Generators

- For scaffolding tasks (creating apps, libs, project structure, setup), ALWAYS invoke the `nx-generate` skill FIRST before exploring or calling MCP tools

## When to use nx_docs

- USE for: advanced config options, unfamiliar flags, migration guides, plugin configuration, edge cases
- DON'T USE for: basic generator syntax (`nx g @nx/react:app`), standard commands, things you already know
- The `nx-generate` skill handles generator discovery internally - don't call nx_docs just to look up generator syntax

<!-- nx configuration end-->
